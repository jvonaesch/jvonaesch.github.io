
title: Jonas Vonäsch
description: 

theme: minima

defaults:
  - scope:
      path: ""
    values:
      pagebar: main
      layout: page
  - scope:
      path: "teaching"
    values:
      pagebar: teaching
      category_title: "Lernmaterial"

plugins:
  - jekyll-feed
  - jekyll-seo-tag

markdown: kramdown
kramdown:
  input: GFM
  syntax_highlighter: rouge
  syntax_highlighter_opts:
    css_class: 'highlight'
