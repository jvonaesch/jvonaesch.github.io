
title: Jonas Vonäsch ETHZ
description: 

theme: minima

defaults:
  - scope:
      path: ""
      #type: "pages"
    values:
      sidebar:
        nav: main
      layout: page
  - scope:
      path: "teaching"
    values:
      sidebar:
        nav: teaching

plugins:
  - jekyll-feed
  - jekyll-seo-tag
