theme: jekyll-theme-minimal
title: Jonas Vonäsch ETHZ
description: 

defaults:
  - scope:
      path: ""        # apply to all pages (adjust path/type as needed)
      #type: "pages"   # optional: apply only to pages
    values:
      sidebar:
        nav: foo
